services:
  db:
    ports:
      - "5432:5432" 
  backend:
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.development
    
    env_file:
      - ../app/.env
    volumes:
      - ..:/workspace
    ports:
      - "8000:8000"
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
